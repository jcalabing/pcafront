import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#14A346",
      contrastText: "#EFF9F0",
    },
    secondary: {
      main: "#FFA400",
      contrastText: "#EFF9F0",
    },
  },
});

export default theme;
