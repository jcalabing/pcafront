const mainStyle = (theme) => ({
  wrapper: {
    position: "relative",
    top: "0",
    display: "flex",
    flexDirection: "column",
    minHeight: "100vh",
    flexGrow: 1,
  },
});

export default mainStyle;
